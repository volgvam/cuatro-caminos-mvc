@model IEnumerable<CuatroCaminosMvcApplication.Models.Administrators>

@{
    ViewBag.Title = "Index";
}

<h2>Администрирование</h2>

<br/>
    @Html.ActionLink( "Работа с группами", "Index", "Group") | 

<br/>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            PeopleID
        </th>
        <th>
            DanceID
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Списки_людей.Фамилия) 
            @Html.DisplayFor(modelItem => item.Списки_людей.Имя)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Названия_танцев.Название_танца)
        </td>
        <td>
@*            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
*@            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
