
@{
    SelectList selectList = ViewBag.selectList ?? new SelectList("");
    String LastName = ViewBag.LastName != null ? ViewBag.LastName.ToString() : "";
    String id = ViewBag.IdPeople != null ? ViewBag.IdPeople.ToString() : "";
    
}

<style>
	.ui-autocomplete-loading { background: white url('/Content/themes/base/images/ui-anim_basic_16x16.gif') right center no-repeat; }
</style>
	<script>
	    $(function () {

	        $("#peoplelastname").autocomplete({
	            source: "@Url.Action("searchJsonPeopleLastName","Service")",
	            minLength: 2,
	            change : function (event, ui) {
	                $(".peoplelastname").val(ui.item ? ui.item.value : this.value);
	            }
	        });
	    });
	</script>




@*	@:помощь в поиске ФИО 
*@
    <input id="peoplelastname" value='@LastName'  class="text-box single-line"/>  
    @Html.TextBox("", @LastName, new { @class = "peoplelastname", style = "width: 1px; visibility:hidden" })

    



@*        @Html.DropDownList("", selectList, "", new { @class = "peopleid", style = "width: 200px; visibility:hidden" })
*@